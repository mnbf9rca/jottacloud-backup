apiVersion: v1
kind: PersistentVolume
metadata:
  name: jottacloud-backup-data-pv
  namespace: jottacloud-backup
spec:
  capacity:
    storage: 2Ti  # Adjust based on your Jottacloud storage size
  accessModes:
    - ReadWriteMany
  nfs:
    server: 10.10.10.1  # Replace with your NFS server
    path: "/tank/largeappdata/jottacloud"  # Replace with your NFS path
  persistentVolumeReclaimPolicy: Retain
  storageClassName: retain-local-path
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jottacloud-backup-data-pvc
  namespace: jottacloud-backup
spec:
  volumeName: jottacloud-backup-data-pv
  accessModes:
    - ReadWriteMany
  storageClassName: retain-local-path
  resources:
    requests:
      storage: 2Ti
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: jottacloud-backup-config-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn
  csi:
    driver: driver.longhorn.io
    volumeHandle: jottacloud-backup-config  # Explicit Longhorn volume name
    fsType: ext4
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jottacloud-backup-config-pvc
  namespace: jottacloud-backup
spec:
  volumeName: jottacloud-backup-config-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn